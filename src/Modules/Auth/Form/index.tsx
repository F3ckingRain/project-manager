import { Password } from "./Fields/Password";

/** Форма авторизации. */
export function AuthForm (): React.JSX.Element {

    return (
        <div>
            authForm

            <Password />
        </div>
    )
}